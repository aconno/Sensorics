buildscript {

    ext.kotlin_version = '1.3.41'

    repositories {
        google()
        jcenter()
        maven { url 'https://maven.fabric.io/public' }
        maven { url "https://kotlin.bintray.com/kotlin-eap" }
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.4.1'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath 'io.fabric.tools:gradle:1.30.0'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        maven { url "https://kotlin.bintray.com/kotlin-eap" }
    }
}

task runDomainUnitTests(dependsOn: [':domain:test']) {
    description 'Run unit tests for the domain layer.'
}

task runDataUnitTests(dependsOn: [':data:cleanTestDebugUnitTest', ':data:testDebugUnitTest']) {
    description 'Run unit tests for the data layer.'
}

task runUnitTests(dependsOn: ['runDomainUnitTests', 'runDataUnitTests']) {
    description 'Run unit tests for both domain and data layers.'
}

task runAcceptanceTests(dependsOn: [':presentation:connectedAndroidTest']) {
    description 'Run application acceptance tests.'
}


task clean(type: Delete) {
    delete rootProject.buildDir
}

ext {
    sensorics_version_code = 31
    sensorics_version_name = '5.2.4'

    compile_sdk_version = 28
    min_sdk_version = 21
    target_sdk_version = 28

    support_library_version = '28.0.0'

    android_arch_version = '1.1.1'

    appcompat_version = '1.0.2'
    cardview_version = '1.0.0'
    recyclerview_version = '1.0.0'
    legacy_support_version = '1.0.0'
    preference_version = '1.0.0'
    androidx_room_version = '2.1.0'

    constraint_layout_version = '1.1.3'
    workManagerVersion = "2.1.0-alpha02"

    javax_annotation_version = '1.0'
    javax_inject_version = '1'

    rx_java_version = '2.2.11'
    rx_android_version = '2.1.1'
    //Current version 2.4.0 not compiling on clean build
    rx_kotlin_version = '2.3.0'

    //Current version 2.24 not compiling on clean build
    dagger_version = '2.23.2'

    material_version = '1.1.0-alpha05'
    flexbox_version = '1.0.0'
    gson_version = '2.8.5'

    jsonwebtoken_version = '0.9.1'

    dfu_version = '1.9.0'

    timber_version = '4.7.1'
    threetenabp_version = '1.2.1'

    mp_android_chart_version = 'v3.1.0'

    colorpicker_version = '0.0.13'

    materialtabstrip_version = '1.1.1'

    circleprogress_version = '1.2.1'

    crashlytics_version = '2.10.1@aar'

    paho_android_service_version = '1.1.1'
    paho_client_mqttv_version = '1.2.1'

    eval_ex_version = '2.1'

    retrofit_version = '2.6.1'
    logging_interceptor_version = '4.0.1'
    //current version 4.0.1 not compiling on clean build
    okhttp3_data_version = '3.14.2'

    assisted_inject_dagger_version = '0.4.0'

    leak_canary_version = '1.6.2'

    junit_version = '4.12'

    coroutines_version = '1.2.2'

    test_runner_version = '1.2.0'
    core_testing_version = '2.1.0-alpha01'
    espresso_core_version = '3.2.0'
    espresso_version = '3.0.2'

    mockito_inline_version = '3.0.0'

    robolectric_version = '4.3'

    google_truth_version = '1.0'
}